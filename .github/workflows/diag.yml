name: DIAG env cross-owner

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/diag.yml'

permissions:
  contents: read

jobs:
  diag:
    uses: spark-match/spark-match-01-devops/.github/workflows/diag-env.yml@diag/test-env-isolation
    with:
      environment-name: ci
